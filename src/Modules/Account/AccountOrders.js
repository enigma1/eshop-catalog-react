import {useStartup, useSequence, useDefined} from '^/Services/useCustomHooks';

const AccountOrders = ({name}) => {

}

AccountOrders.defaultProps = {
  name: AccountOrders.name,
}

export default AccountOrders;
