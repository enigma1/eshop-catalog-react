import {useStartup, useSequence, useDefined} from '^/Services/useCustomHooks';

const AddressList = ({name}) => {

}

AddressList.defaultProps = {
  name: AddressList.name,
}

export default AddressList;
